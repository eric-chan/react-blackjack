import { chakra } from "@chakra-ui/react";
import React from "react";
import Alert from "@chakra-ui/react";

const StatusFlash = ({ winner }) => {
  return <h2>{winner}</h2>;
};

export default StatusFlash;